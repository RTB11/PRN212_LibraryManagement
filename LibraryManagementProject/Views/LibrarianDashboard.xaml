<Window x:Class="LibraryManagementProject.Views.LibrarianDashboard"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LibraryManagementProject.Views"
        mc:Ignorable="d"
        Title="LibrarianDashboard" Height="450" Width="800">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="#34495E">

            <TextBlock Text="LIBRARY MANAGEMENT SYSTEM"
                       Foreground="White"
                       FontSize="24"
                       FontWeight="Bold"
                       VerticalAlignment="Center"
                       Margin="20,0,0,0"/>

        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0"
                        Background="#2C3E50">

                <Button x:Name="btnDashboard"
                        Content="Dashboard"
                        Height="45"
                        Margin="10"/>

                <Button x:Name="btnBorrow"
                        Content="Borrow Book"
                        Height="45"
                        Margin="10"/>

                <Button x:Name="btnReturn"
                        Content="Return Book"
                        Height="45"
                        Margin="10"/>

                <Button x:Name="btnMembers"
                        Content="Members"
                        Height="45"
                        Margin="10"/>

                <Button x:Name="btnSearch"
                        Content="Search Book"
                        Height="45"
                        Margin="10"/>

                <Button x:Name="btnHistory"
                        Content="Borrow History"
                        Height="45"
                        Margin="10"/>

                <Button x:Name="btnLogout"
                        Content="Logout"
                        Height="45"
                        Margin="10"/>

            </StackPanel>

            <!-- Nội dung -->
            <Frame x:Name="MainFrame"
                   Grid.Column="1"
                   NavigationUIVisibility="Hidden"/>

        </Grid>

    </Grid>

</Window>
